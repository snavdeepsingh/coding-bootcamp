SELECT field_names, field_name FROM table_name 
WHERE field_name = CONDITIONAL AND field_name = CONDITIONAL

INSERT INTO table_name(
	field_names,
	field_name
)
VALUES(
 VALUE_1,
 VALUE_2
)

UPDATE table_name SET field_name = value, field_name = value WHERE field_name = CONDITIONAL

DELETE FROM table_name WHERE field_name = conditional

